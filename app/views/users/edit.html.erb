<div class="edit">
<%= form_for @user, url: user_path(@user), html: {method: "patch"} do |f| %>

 <div class="form-group">
  <%= f.label "お名前"  %>
  <%= f.text_field :name, :maxlength => 30, :class => "form-control" %>
 </div>

 <div class="form-group">
  <%= f.label "自己紹介" %>
  <%= f.text_area :description, :maxlength => 160, :class => "form-control" %>
 </div>

 <div class="form-group ">
  <%= f.label "背景画像"%>
  <%= f.file_field :picture %>
  <p>※必須ではありません.</p>
 </div>

 <div class="form-group">
  <%= f.label "職業" %>
  <%= f.text_field :type, :maxlength => 15, :class => "form-control" %>
  <p>※必須ではありません.</p>
 </div>

 <button type="submit" class="btn btn-primary">変更</button>

<% end %>
</div>
